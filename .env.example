# Backend API origin (no trailing slash, no /api path). Injected at build time by Vite.
VITE_API_URL=example.com

# Optional: Socket.io origin if it differs from VITE_API_URL (defaults to VITE_API_URL).
# VITE_SOCKET_URL=https://api.example.com
